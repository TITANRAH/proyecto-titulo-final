import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'organizador_clases',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
